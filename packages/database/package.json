{
  "name": "@penshop/database",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit && prisma generate && tsc --build",
    "generate": "prisma generate",
    "push": "prisma db push",
    "studio": "prisma studio",
    "seed": "tsx src/seed.ts",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:reset": "prisma migrate reset"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.22",
    "@prisma/client": "^6.5.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@nestjs/core": "^10.4.22",
    "@types/node": "^25.5.0",
    "prisma": "^6.5.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
